# Project brief

Solo, six client repositories, one way of working across all of them.

Work never starts from a bare prompt: a request becomes a spec, the spec becomes
tasks, one task per worktree. Four run at a time, up to seven when tracks overlap.

What is never delegated: client credentials, IAM policy, the first migration on
a table I have not read, pricing logic.
