# Version control

- Conventional commits, scoped by package.
- One pull request, one intent. A refactor rides alone.
- The plan lives in `aidd_docs/tasks/<pr>-<slug>.md` and is written before the
  branch exists.
- Squash on merge. The branch history is working notes, not a record.
